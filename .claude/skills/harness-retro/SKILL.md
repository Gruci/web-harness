---
name: harness-retro
description: 하네스 회고 — 훅이 막은 기록(harness_trace.jsonl)을 패턴으로 읽고, 규칙 위반인지 게이트 오탐인지 규칙이 틀린 건지 판정해 반영한다. "[정비] harness-retro" 알림이 떴을 때, 그리고 "회고 돌려줘"·"게이트가 자꾸 걸린다"·"오탐 같다"·"하네스 손보자" 요청 시 반드시 이 스킬을 사용할 것. 채굴은 기계가, 판정은 사람이 한다.
---

# harness-retro — 관찰을 규칙으로 바꾸는 회고

> 담는 것: 회고 절차와 판정 기준. 담지 않는 것: 관찰 스키마(→ `kernel/trace.py`)·집계 로직(→ `kernel/retro.py`)·하네스 전체 지도(→ `dev/HARNESS.md`). 읽는 시점: 정비 알림이 떴을 때, 그리고 게이트가 반복해서 걸릴 때.

훅이 막을 때마다 `harness_trace.jsonl` 에 한 줄이 쌓인다. 이 스킬은 그 기록을 읽고 **하네스를 이 프로젝트에 맞게 조인다.** 게이트는 설계된 게 아니라 사고마다 하나씩 늘었고, 이 회고가 그 순환의 채굴 단계다.

## 절차

1. **채굴** — `python -X utf8 -m kernel.retro`. 게이트별 빈도와 반복 지점과 게이트 밖 마찰이 나온다.
2. **현재 상태** — `python -X utf8 -m kernel.runner`. 지금 남아 있는 위반과 대조한다. 회고 시점에 이미 고쳐진 것과 아직 살아 있는 것은 다른 이야기다.
3. **판정** — 패턴마다 셋 중 하나다. 이건 기계가 못 한다.
4. **제안** — 판정별 조치를 만든다.
5. **승인** — `AskUserQuestion` 으로 묻는다. 조치는 되돌리기 비싼 하네스 변경이라 자율 진행 금지 구간이다.
6. **반영** — 승인분만.
7. **검증** — `python -X utf8 tests/run_golden.py` 와 `--bare` 둘 다.
8. **기록** — 거절분과 스탬프.

## 3. 판정 — 이게 이 스킬의 본체다

| 판정 | 무엇을 보고 아나 | 조치 |
|---|---|---|
| **규칙 위반** | 게이트가 옳았고 내가 어겼다. 위반이 여러 파일에 흩어져 있다 | `dev/LESSONS.md` 에 절을 더하고 승격 여부를 그 자리에서 판단한다 |
| **게이트 오탐** | 위반이라는데 코드가 실제로 멀쩡하다. 같은 파일에서 반복된다 | `kernel/gates/` 의 판정을 고친다. 이 레포에서만 참이면 `harness_gates/` 다 |
| **규칙이 틀렸다** | 게이트는 정확히 작동하는데 그 규칙이 이 프로젝트에 안 맞는다 | 프로파일의 어휘·임계를 고친다. 규칙 자체를 뺄 거면 `dev/REJECTED.md` 에 사유를 남긴다 |

**반복 지점이 제일 값싼 신호다.** 같은 게이트가 같은 파일에서 세 번 이상 걸렸다면 둘 중 하나다 — 그 파일의 관례가 아직 안 정해졌거나, 게이트가 그 파일에서 오탐을 낸다. `dev/CONVENTIONS.md` 에 관례를 등재하는 것으로 끝나는 경우가 많다.

**게이트 밖 마찰도 신호다.** `context_diet` 가 잦으면 정본 MD 가 비대해진 것이고, `editing_lock` 이 잦으면 완료 보고를 빠뜨리는 것이고, `gate_error` 는 게이트가 조용히 안 돌고 있다는 뜻이라 제일 급하다.

## 4~5. 제안과 승인

**면제 목록 추가는 조치가 아니다.** `ALLOWLIST` 와 `SCOPE["exclude_all"]` 를 늘리는 제안은 하지 않는다. 편집 표면 래칫(`harness_gates/edit_surface.py`)이 막고, 그게 이 루프의 안전장치다. 적합도 함수가 없는 상태에서 그 문을 열면 루프는 가장 싼 통과 경로로 수렴한다 — 게이트를 무르게 만들기다.

정말 필요하면 `dev/REJECTED.md` 에 대안 검토 결과를 먼저 적고, 사용자 승인 후 `harness_surface.txt` 에 행을 더한다. 그 한 줄이 사람이 보는 지점이다.

## 6. 반영 — 새 게이트를 만들었다면

통과 확인만으로는 배선된 것으로 보지 않는다. **합성 입력으로 실제 발화까지 확인한다.** 꺼진 게이트와 배선 빠진 게이트와 대상이 없어 통과한 게이트는 화면상 전부 같다 — 경위는 `dev/LESSONS.md` §15 다.

## 7. 검증 — 순서를 지킨다

게이트를 고쳤으면 **정답지를 먼저 갱신하지 않는다.** 먼저 대조를 돌려 무엇이 달라졌는지 눈으로 읽고, 그 변화가 의도한 것일 때만 `--update` 한다. 정답지를 먼저 갱신하면 회귀 검출이 통째로 무의미해진다.

## 8. 기록

- 거절·보류한 제안을 `dev/REJECTED.md` 에 적는다. **부정 결과가 사라지면 다음 회고가 같은 제안을 다시 한다.**
- `python -X utf8 -m kernel.maintenance --stamp harness-retro`
- `harness_maintenance.json` 과 `harness_trace.jsonl` 을 커밋한다. 스탬프 날짜가 다음 회고의 기준점이라, 안 남기면 같은 기록을 계속 다시 읽는다.
