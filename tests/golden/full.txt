exit=1
[REPORT] DEVGUIDE.md: ⑬e 날짜 태그 6개 — 성분 D 의심(경위는 dev/LESSONS.md 로)
[OK]   프로파일 형식

[FAIL] 파일 길이 상한 (line_limit) — 1건
   - utils/long_report.py: 401줄 (>400)

[FAIL] 헤더 경로 주석 (header_path) — 1건
   - utils/moved.py: 헤더 주석 'utils/old_name.py' ≠ 실경로 — 주석을 실경로로 갱신

[FAIL] 중첩 def(클로저) (closures) — 1건
   - utils/closure.py:5: 중첩 def outer > inner (불가피하면 `# closure-ok: 사유`)

[FAIL] 함수 길이 상한 (func_limit) — 1건
   - utils/long_func.py:4: long_calc 82줄 (>80)

[FAIL] TYPE_CHECKING↔future annotations 짝 (type_checking_future) — 1건
   - utils/tc_future.py: TYPE_CHECKING 블록이 있는데 `from __future__ import annotations` 가 없다 — 3.11 은 어노테이션을 즉시 평가해 NameError

[FAIL] 읽기 레이어의 쓰기 SQL·commit (reads_writes) — 1건
   - db/reads/bad_write.py:5: 쓰기 SQL — conn.execute("DELETE FROM cache")

[FAIL] 읽기 레이어 컬럼 식별자 raw 보간 (reads_col_interp) — 1건
   - db/reads/col_interp.py:5: 컬럼 식별자 raw 보간 — 바인딩 불가 자리라 새면 SQLi 다. 화이트리스트 헬퍼(quote_col 류) 경유, 헬퍼 정의 파일은 ALLOWLIST['sql_ident'] 등재 — sql = f'SELECT "{column}" FROM metric'

[FAIL] 쓰기 레이어 round() 절삭 (writes_round) — 1건
   - db/writes/loader.py:5: 적재 값 round() 절삭 — 저장은 원 정밀도, 반올림은 표시 레이어에서 — conn.execute("INSERT INTO metric VALUES (?)", (round(value, 

[FAIL] 배치 직접 SELECT (batch_select) — 1건
   - batches/report.py: 배치 안 직접 SELECT — 조회는 db/reads/ 경유

[FAIL] 축약 이름 단독 대입 (abbrev_names) — 1건
   - utils/abbrev.py:3: 축약어 변수 net — net = 0

[FAIL] 축약 접두 식별자 (abbrev_prefixes) — 1건
   - utils/abbrev.py:4: 축약 접두 oper_ — oper_income = 1

[FAIL] UI 라벨 금칙어 (ui_jargon) — 1건
   - frontend/src/Label.tsx:1: UI 금칙어 '순신고가' — export const title = "순신고가";

[FAIL] Any 타입힌트 (py_any) — 1건
   - utils/anyhint.py:5: 임의 타입 → 구체 타입 (불가피하면 `# any-ok: 사유`)

[FAIL] 공개 함수 타입힌트 (type_hints) — 11건
   - db/conn_loop.py:4: agg() 타입힌트 누락 — 반환
   - db/reads/bad_write.py:4: wipe() 타입힌트 누락 — conn, 반환
   - db/reads/core_import.py:5: rows() 타입힌트 누락 — 반환
   - kofia/collect.py:4: collect() 타입힌트 누락 — 반환
   - utils/closure.py:4: outer() 타입힌트 누락 — 반환
   - utils/closure.py:10: wrapped() 타입힌트 누락 — 반환
   - utils/closure.py:5: inner() 타입힌트 누락 — 반환
   - utils/closure.py:11: keeper() 타입힌트 누락 — 반환
   - utils/ssl_utils.py:4: bypass_ssl_verification() 타입힌트 누락 — 반환
   - web/handlers.py:4: ping() 타입힌트 누락 — 반환
   - web/routes/errors.py:4: fail() 타입힌트 누락 — 반환

[FAIL] 시크릿 토큰 하드코딩 (secrets) — 1건
   - batches/leak.py:3: 시크릿 토큰 하드코딩 — 설정 모듈 경유로 옮기고, 이미 커밋됐다면 키를 회전하라
[TOOL] TS any 타입 — frontend/node_modules/.bin/eslint 없음 — 설치: npm i -D eslint @typescript-eslint/parser typescript

[FAIL] 커넥션 블록 내 가공 (conn_processing) — 1건
   - db/conn_loop.py:5: 커넥션 블록 내 중첩 루프 집계 — fetch 후 블록 밖에서 가공하라

[FAIL] 설정 밖 환경변수 조회 (env_access) — 1건
   - utils/env.py:4: settings.py 밖에서 환경변수 조회 — TOKEN = os.getenv("TOKEN")

[FAIL] await 없는 async 핸들러 (web_async) — 1건
   - web/handlers.py:4: await 없는 async def 'ping' — 동기 def 로

[FAIL] 커넥션 접근자 import 단일 경로 (accessor_import) — 1건
   - db/reads/core_import.py:2: get_db 를 db.connection 밖에서 import — from db.core import get_db

[FAIL] 전역 SSL 패치 호출 위치 (ssl_bypass) — 1건
   - web/patch.py:4: 전역 SSL 패치를 진입점 밖에서 호출 — bypass_ssl_verification()

[FAIL] 라우트 에러 응답 형식 (routes_error) — 1건
   - web/routes/errors.py:5: 에러를 JSONResponse(status 404) 로 반환 — 예외로 올려라
[TOOL] 공용 래퍼 없는 fetch — frontend/node_modules/.bin/eslint 없음 — 설치: npm i -D eslint @typescript-eslint/parser typescript
[TOOL] 프론트 색 리터럴 — frontend/node_modules/.bin/eslint 없음 — 설치: npm i -D eslint @typescript-eslint/parser typescript
[TOOL] 폰을 깨뜨리는 고정 폭 — frontend/node_modules/.bin/eslint 없음 — 설치: npm i -D eslint @typescript-eslint/parser typescript
[TOOL] 브라우저 API 직접 호출 — frontend/node_modules/.bin/eslint 없음 — 설치: npm i -D eslint @typescript-eslint/parser typescript
[TOOL] 해시 네비게이션 단일 기전 — frontend/node_modules/.bin/eslint 없음 — 설치: npm i -D eslint @typescript-eslint/parser typescript

[FAIL] 프론트 로직 테스트 짝 (ui_logic_tests) — 1건
   - frontend/src/calcShare.ts: 대응 행동 테스트 없음 — calcShare.test.ts 를 같은 자리에 작성

[FAIL] 프론트 컴포넌트 테스트 짝 (ui_component_tests) — 1건
   - frontend/src/Label.tsx: 대응 렌더 테스트 없음 — Label.test.tsx 를 같은 자리에 작성

[FAIL] 루트 직속 잡파일 (root_litter) — 1건
   - notes_dump.txt: 루트 직속 파일 금지 — 읽는 코드의 패키지 안에 두고, 루트가 맞으면 사유와 함께 프로파일 ROOT_FILES 에 등재하라
[SKIP] 프롬프트 버전 범프 — 설정에 버전 관리 프롬프트 목록을 안 적었음

[FAIL] 앱 코드 배치 (file_placement) — 2건
   - kofia/: 도메인 패키지에 __init__.py 없음 — 생성하라
   - kofia/: 도메인 정본 MD 없음 — kofia/KOFIA.md 를 만들고 허브에서 링크하라 (고아 MD 게이트가 도달성을 본다)

[FAIL] 수집·계산 모듈의 행동 테스트 짝 (test_pairing) — 1건
   - kofia/collect.py: 대응 행동 테스트 없음 — tests/test_collect.py 작성 또는 사유와 함께 test_pairing_baseline.txt 등재

[FAIL] DDL 저장 타입 잘림 (ddl_types) — 1건
   - db/schema/tables.py:5: amount REAL — DOUBLE PRECISION(실수)·BIGINT(정수)·NUMERIC(고정소수) 중 하나로. float4 는 2^24 초과 정수를 못 담는다

[FAIL] API 응답 배열 필드 옵셔널 (api_array) — 1건
   - frontend/src/types/api.ts:items: API 응답 타입의 배열 필드는 옵셔널(`?`)로 — 백엔드 배포가 늦으면 undefined 로 도착해 소비처가 크래시한다. 필수로 둬야 하면 baseline 에 등재

[FAIL] 소비 UI 없는 API 라우트 (orphan_api) — 1건
   - web/routes/orphan.py:9: `/api/nobody-consumes-this` — 소비하는 화면 코드가 없다. 컴포넌트까지가 한 단위다

[FAIL] 미정의 모듈 상수 (undefined_const) — 1건
   - utils/stale_const.py:6: 미정의 모듈 상수 DEFAULT_LIMIT — 개명·삭제에서 소비처를 놓쳤다. 호출 시점 NameError 가 된다

[FAIL] 선언 본문 중복(정본 재구현) (dup_decl) — 1건
   - utils/money_a.py:4: 선언 본문 동일(signed_won/won_signed) — 파일 2개가 같은 구현을 들고 있다. 공용 모듈로 뽑고 한 벌만 남긴다. 위치: utils/money_a.py:4, utils/money_b.py:4
[OK]   블록 중복(선언 안 복붙)

[FAIL] MD 작성 규칙 (md_style) — 4건
   - DESIGN_GUIDE.md:5: ⑬a 코드펜스 트리 덤프 — 파일 목록의 정본은 Glob
   - README.md:7: ⑬b 한 줄에 설명 붙은 나열 7개 — 표나 불릿으로 쪼개라
   - README.md:9: ⑬c 괄호 3중 중첩 — 문장을 다시 써라
   - notes/GUIDE.md:1: ⑬d 머리 역할 계약 누락 — `> 담는 것: … 담지 않는 것: … 읽는 시점: …`

[FAIL] MD 경로 참조 실존 (md_path_refs) — 1건
   - AGENTS.md:5: 실존하지 않는 경로 참조 `db/reads/gone.py`

[FAIL] 고아 MD(허브 도달 불가) (md_orphans) — 1건
   - notes/GUIDE.md: 고아 — 허브에서 도달 불가. 라우팅표에 등재하거나 폐기하라

[FAIL] 하네스 지도 대조 (md_harness_map) — 1건
   - HARNESS.md: 스킬 'runbook' 이 지도에 없음 — 같은 턴에 등재하라

[FAIL] 에이전트 모델 정책 (agent_model) — 2건
   - .claude/agents/auditor.md: model 'sonnet' ≠ 정책 'opus' — 라우팅을 바꾼 거면 프로파일을 같은 커밋에 고쳐라
   - .claude/agents/auditor.md: effort 'medium' ≠ 정책 'high' — 판단 상향·하향이면 정책을 같은 커밋에 고쳐라

[FAIL] 사고 절 승격 상태 (lessons_promotion) — 1건
   - dev/LESSONS.md:5: §1 에 `> 강제:` 선언이 없다 — 게이트를 적거나 `산문 전용 — 사유` 로 적어라
[OK]   MD 함수 참조 실존

[FAIL] 아키텍처 그림 1:1 대조 (arch_diagram) — 6건
   - docs/architecture/app.architecture.json: revision 0000000 이 이 레포에 없음 — deliver 를 다시 돌려 HEAD 로 찍는다
   - docs/architecture/app.architecture.json: 노드 reads → db/reads/bad_write.py 행 99 없음 (파일은 5줄)
   - docs/architecture/app.architecture.json: 노드 gone → utils/gone.py 실존하지 않음 — 이름을 바꿨으면 그림도 고친다
   - docs/architecture/app.architecture.json: 노드 orphan 에 sources 없음 — 코드 어디인지 적어야 그림이다
   - docs/architecture/app.architecture.json: utils/ 를 가리키는 노드 없음 — 레이어·도메인 패키지는 전부 그린다
   - docs/architecture/app.architecture.json: 영수증 없음 — `python -X utf8 -m kernel.diagram deliver` 로 렌더한다
[TOOL] 그림 엔진 진단(docs/architecture/app.architecture.json) — node 없음 — node 를 설치하면 켜진다

[FAIL] 문서↔코드 대조(DEVGUIDE.md↔batch_runner.py) (doc_sync:DEVGUIDE.md) — 1건
   - DEVGUIDE.md:9: BATCH_HOUR=5 인데 문서 값 [0, 3] 불일치

[FAIL] 문서↔코드 대조(DEVGUIDE.md↔settings.py) (doc_sync:DEVGUIDE.md) — 2건
   - settings.py 가 읽는 'ALPHA_KEY' 가 DEVGUIDE.md 목록에 없음
   - DEVGUIDE.md 의 'BETA_KEY' 를 settings.py 가 읽지 않음(타 모듈 로드 가능 — 확인)

총 61건 위반.
