# dev/REJECTED.md — 검토하고 채택하지 않은 것

> 담는 것: 제안됐으나 거절되거나 보류된 것과 그 사유. 담지 않는 것: 사고 경위와 규칙의 배경(→ `dev/LESSONS.md`)·채택된 관례(→ `dev/CONVENTIONS.md`). 읽는 시점: 같은 것을 다시 제안하기 전, 그리고 회고에서 거절 판정을 내린 직후.

부정 결과를 안 남기면 다음 세션이 같은 제안을 다시 하고, 같은 논의를 다시 하고, 이번엔
통과시킬 수도 있다. **거절은 결론이지 침묵이 아니다.**

`dev/LESSONS.md` 와 섞지 않는다. 저긴 사고 연대기이고 절마다 `> 강제:` 로 무엇이 그 사고를
막는지 선언해야 한다. 거절된 제안은 사고가 아니고 막을 것도 없다.

날짜는 적지 않는다 — `git log` 가 정본이다. 다시 제안될 때 필요한 것은 날짜가 아니라 사유다.

## 판정

| 판정 | 뜻 |
|---|---|
| 거절 | 이 프로젝트에선 하지 않는다. 전제가 바뀌지 않는 한 재론 대상이 아니다 |
| 보류 | 지금은 아니다. **언제 다시 보는지**를 사유에 적는다 |

## 목록

| 제안 | 나온 곳 | 판정 | 사유 |
|---|---|---|---|
| 회고의 판정과 제안을 자동화 — 채굴 결과에서 게이트 수정안까지 기계가 낸다 | 하네스 폐쇄 루프 설계 | 거절 | 이 하네스엔 "더 좋아졌다"를 재는 적합도 함수가 없다. 골든 대조는 회귀만 잡고 개선은 못 잰다. 적합도 없이 제안과 수용을 자동화하면 가장 싼 통과 경로인 면제 목록 늘리기로 수렴한다. 적합도 함수가 생기면 그때 재론한다 |
| 픽스처 프로파일을 `git add -f` 로 강제 커밋해 골든을 복구 | 하네스 폐쇄 루프 리서치 | 거절 | 동작은 한다. 다만 `.gitignore` 에 걸린 파일이 인덱스에만 살아 있는 상태라, 다음 사람이 `tests/fixture_files.py` 를 읽고 "이건 커밋 안 되는 파일"이라고 정확히 결론 내린 뒤에도 실제로는 커밋돼 있다. `tests/run_golden.py` 가 없으면 다시 짓는 쪽은 그 의존관계가 코드에 드러난다 |
| 편집 표면 래칫을 `MD["style_exclude"]` 와 `harness_baseline.txt` 증가까지 확대 | 하네스 폐쇄 루프 설계 | 보류 | 같은 성격의 구멍이 맞다. 다만 지금 넣으면 실제로 새는지 모른 채 넓히는 것이다. 루프가 돌면 관찰에 드러나고, 그때 근거를 갖고 올린다 |
| `upgrade/` 스냅샷을 도메인 패키지로 정식 등재(`__init__` + 정본 MD) | 실운영본 역이식 비교 작업 | 거절 | `upgrade/` 는 fund_monitor 실운영 하네스의 통짜 사본 — 역이식 비교 재료이지 이 레포의 앱 코드가 아니다. 등재하면 남의 프로젝트 규칙·도메인 문서가 정본 그래프에 유입된다. `SCOPE["exclude_all"]` 면제 + gitignore 미추적이 맞다. 역이식이 끝나면 폴더째 지운다 |
| CSS 캐시버스터 내용 해시 게이트(실운영 ㉖) 백포트 | 2차 역이식 리서치 | 거절 | 프리셋 스택(Vite React)은 번들러가 자산 파일명에 해시를 박는다 — 손으로 `?v=` 를 붙이는 서버 렌더 전제의 게이트라 켜질 프로젝트가 없다. 손 링크 CSS 프로젝트가 생기면 그 레포의 `harness_gates/` 에 심는다 |
| 인라인 `min(max(` 클램프 게이트(실운영 ㉕A) 백포트 | 2차 역이식 리서치 | 보류 | 선언 본문 중복 게이트(dup_decl)가 같은 부류의 재구현을 이미 잡는다. 클램프 인라인이 dup_decl 을 빠져나가는 실누출이 관측되면 그때 올린다 |
| 검증 영수증 게이트 — 마지막 테스트 실행 뒤 소스가 바뀌었으면 Stop 경고 | 하네스 평가 | 보류 | 프록시 판정이라 차단은 못 주고, 경고는 적어뒀다는 이유로 지켜지지 않는다(§20). §11 빈도를 재는 관찰 장치로서만 가치가 있다. 관찰 기록에 §11 이 실제로 반복되는 게 보이면 그때 넣는다 |
| 규모별 패스트트랙 — 단일 레이어 소규모면 plan 파일 없이 채팅 승인 | 하네스 평가 | 거절 | 규모는 plan 을 짜봐야 안다 — 사전 기준이 성립하지 않는다. "간단해 보인다"가 §4 사고의 원인이고, 예상 줄 수 기준은 패스트트랙에 들어가려는 낙관 추정을 부른다 |
| 게이트 소거 기준(적중 0건 → 소거 후보)과 신규 게이트 동결 | 하네스 평가 | 거절 | 적중 0건은 "잘 지켜진다"와 "필요 없다"를 구분 못 한다. 예방 게이트(21·47)는 적중 0이 정상이다. 지우면 다음 사고를 리팩토링 때나 알게 된다. 동결은 사고→게이트 루프를 끄는 것이다 |
| API 응답 스키마 ↔ 프론트 타입 필드 대조 게이트 | 백로그 전수 대조 | 보류 | 정적 분석으로 성립하지 않는다. 이 하네스가 강제하는 레이어 규율이 라우트를 `return <래퍼>(...)` 로 밀기 때문에 **간접 반환이 예외가 아니라 기본값**이고, 커널엔 크로스모듈 심볼 해석이 없어 응답 키 집합을 못 뽑는다. 소비처 스캔은 같은 부류에서 이미 오탐 43건이 실측됐다. 결정타는 `/api/board` ↔ `interface Board` 를 잇는 근거가 레포에 없다는 것이다. **라우트↔타입을 선언하는 앵커 관례(핸들러 주석 등)가 먼저 생기면** 그때 리터럴 반환 라우트로 좁혀 재론한다 |
| pytest 를 Stop 훅에서 실행 | 백로그 전수 대조 | 보류 | 전량이 **4분**(67건 211초)인데 매 턴 도는 Stop 훅의 현행 최장 timeout 이 120초다. 넣으려면 변경→테스트 매핑이라는 새 기계가 필요하고, 그 기계가 수동 실행보다 비싸다. 게다가 Stop 은 이미 전 게이트(⑬)를 돌린다. **변경 대비 테스트가 안 돌아 회귀가 새어나간 사례가 관측되면** 그때 선별 실행으로 재론한다. 같은 부류의 보류가 위 「검증 영수증 게이트」다 |
| 예외 목록(allowlist)을 프로파일 밖 별도 파일에서 읽는 통로 | 백로그 전수 대조 | 거절 | 지금 이 통로를 쓸 프로젝트가 없다. 목록이 수십 개로 불어난 레포는 이 커널을 아직 안 쓰고, 그 레포가 옮겨올 때 **자기 목록의 모양을 보고** 정하는 편이 낫다. 지금 넣으면 소비처 0인 로더가 커널에 남고, 그 로더가 곧 "면제를 파일로 빼는 싼 경로"가 된다 — 편집 표면 래칫이 막으려는 바로 그 방향이다 |
| 화면 없는 프로젝트에서 `impeccable` 벤더 스킬 자동 제외 | 백로그 전수 대조 | 거절 | 비용 실측이 102파일 **2.14MB 디스크 + 세션당 설명 1줄**이다. 지우려면 설치 시점에 파괴적 단계와 "하네스 자신은 지우지 않는다" 가드가 필요한데, 그 기계가 아끼는 것보다 크다. 업그레이드 경로는 `.claude/skills/` 를 건드리지도 않으므로 한 번 지우면 끝이다 — 쓸 일 없는 프로젝트는 손으로 지운다 |
