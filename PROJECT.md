# PROJECT.md — 이 프로젝트가 무엇인가

> 담는 것: 이 서비스가 다루는 것·스택·레이어 배치·이 바닥의 용어. 담지 않는 것: 세션 행동 규칙(→ `CLAUDE.md`)·하네스 실물 지도(→ `dev/HARNESS.md`)·코드 관례(→ `dev/CONVENTIONS.md`). 읽는 시점: 세션 시작, 그리고 이 프로젝트가 무엇을 하는 건지 물을 때마다.

**새 프로젝트가 채우는 문서다.** `CLAUDE.md` 는 어느 프로젝트에서든 같은 골격이고, 여기만
프로젝트마다 다르다. 하네스를 옮길 때 갈아끼우는 것은 이 파일과 `harness_profile.py` 둘이다.

미리 다 채우지 않는다. 개발하면서 쌓인다.

## 무엇을 만드나

<!-- 한 문단. 누가 무엇 때문에 쓰는 서비스인지. 기능 목록이 아니라 목적을 쓴다. -->

TODO — `harness-init` 스킬이 0단계 인터뷰에서 물은 답을 여기 옮긴다.

## 스택

| 층 | 무엇 |
|:--|:--|
| 서버 | TODO |
| DB | TODO |
| 화면 | TODO |
| 배포 | TODO |

스택을 바꾸면 `harness_profile.py` 의 `LAYERS` 와 `SYMBOLS` 를 같은 커밋에 고친다.
프레임워크마다 커넥션 헬퍼 이름과 에러 응답 래퍼가 다르고, 게이트는 그 이름으로 대상을 고른다.

## 레이어

`harness_profile.py` 의 `LAYERS` 가 기계용 정본이고, 여기는 사람용 설명이다. 둘이 어긋나면
배치 게이트(검사 21)가 잡는다.

| 경로 | 역할 | 넣지 않는 것 |
|:--|:--|:--|
| TODO | | |

의존성 방향을 한 줄로 적는다. 역방향 import 는 금지다.

## 도메인 패키지

최상위에 소문자 패키지를 만들면 그 순간 도메인 패키지가 된다. 등재는 없고 **모양이 계약**이다
— `__init__.py` 와 동명 정본 MD 를 같은 턴에 만든다. 없으면 검사 21 이 막고, 그 MD 가 허브에서
도달 불가면 검사 27 이 고아로 잡는다.

| 패키지 | 다루는 것 | 정본 MD |
|:--|:--|:--|
| — | 아직 없음 | |

## 용어

이 바닥에서만 통하는 말과, 사용자에게 보여선 안 되는 내부어를 적는다. 내부어는
`harness_profile.py` 의 `VOCAB["ui_denylist"]` 에도 넣는다 — 그래야 화면에 새는 것을 게이트가 막는다.

| 용어 | 뜻 | 화면 노출 |
|:--|:--|:--|
| — | | |

## 의미 있는 숫자

이 도메인에서 "이 값이 이러면 이상하다"고 말할 수 있는 기준들. 검증 로직과 리뷰의 근거가 된다.

| 값 | 정상 범위 | 벗어나면 |
|:--|:--|:--|
| — | | |
