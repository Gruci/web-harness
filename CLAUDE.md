# CLAUDE.md — Claude Code 세션 행동 규칙

> 담는 것: 세션 행동 규칙과 무엇을 언제 읽을지의 라우팅. 담지 않는 것: 이 프로젝트가 무엇인지(→ `PROJECT.md`)·하네스 실물 지도(→ `dev/HARNESS.md`)·규칙이 생긴 경위(→ `dev/LESSONS.md`)·코드 관례(→ `dev/CONVENTIONS.md`). 읽는 시점: 세션 자동 주입.

> **이 파일은 스택을 모른다.** 어느 프로젝트에서든 같은 골격이고, 프로젝트마다 다른 것은 `PROJECT.md`(사람용)와 `harness_profile.py`(기계용) 둘이다. 하네스를 옮길 때 갈아끼우는 것도 그 둘이다.
> 메인 깃(origin)은 초기 설정 ⓪에서 연결한다 — 연결된 그 레포가 이 프로젝트의 유일한 정본이다.

## Codex 병행 운용 경계

- Claude Code 전용 진입점은 이 파일과 `.claude/`다. `AGENTS.md`와 `.agents/`와 `.codex/`는 Codex 전용이라 일반 작업에서 읽거나 수정하지 않는다. 상호운용 유지보수를 명시 요청받은 경우만 예외다.
- 개발과 설계의 정본은 양쪽이 공유한다.
- 작업 절차는 [공통 절차 허브](dev/workflows/README.md)에서 해당 문서만 읽는다.
- Claude 스킬은 공통 절차에 도구 호출과 역할 배정만 연결한다.
- 공통 저장·종료 검사의 연결과 런타임별 차이는 [dev/HARNESS.md](dev/HARNESS.md)를 따른다.

---

## 초기 설정 (clone 후 1회)

**⓪ git + GitHub 원격 확인 — 모든 코딩보다 최우선.** ⓪은 로컬 `git init`으로 끝나지 않는다. **GitHub 원격 연결과 최초 푸시까지가 ⓪이다.**

`.claude/hooks/check_git_remote.py`가 Stop 훅으로 강제하고(fail-closed) 미설정 시 절차를 직접 출력한다.

**① 하네스 연결 — 코딩 전.** `harness_profile.py`가 없으면 커널이 이 프로젝트에 대해 아무것도 모르는 상태다 — 검사 경로를 요구하는 게이트가 전부 `[SKIP]`이고, `[SKIP]`은 통과가 아니다(「일관성 게이트」1). `/harness-init` 스킬이 온보딩 인터뷰부터 꺼진 게이트 확인까지 처리하고, 절차 정본은 [조립 절차](dev/workflows/harness-assembly.md)다.

**스택은 사용자가 고른다.** 먼저 무엇을 만드는지 묻고, 언어와 프레임워크가 미정이면 후보의 실제 제약을 설명한 뒤 선택을 요청한다. 기술 이름만 던지는 질문("FastAPI 쓸래 Django 쓸래")은 답을 아는 사람만 답할 수 있으므로, 후보마다 무엇이 달라지는지를 같이 말한다. 이미 정한 것은 다시 묻지 않는다.

**② 글로벌 퍼미션 — clone 직후 즉시.** `~/.claude/settings.json`에 전체 allow가 없으면 코딩 전에 `python -X utf8 setup_global_permissions.py`를 실행한다. 기존 글로벌 설정은 보존하고 permissions만 병합한다. 프로젝트 로컬 설정만으로는 프로젝트 밖 작업과 새 세션 첫 턴에서 프롬프트가 계속 뜬다.

Codex도 함께 초기화하는 요청이면 `python -X utf8 setup_global_permissions.py --agent both`를 사용한다.
각 실행 환경의 전역 설정만 병합하며 Codex의 적용 여부는 새 세션에서 확인한다.

---

## 작업 전 필독 라우팅

**파일 편집 전 아래 표의 해당 MD를 먼저 Read한다.** import 없이 on-demand로 읽는다.

| 작업 대상 | 읽어야 할 MD |
|-----------|-------------|
| **이 프로젝트가 뭐 하는 건지 모를 때** | `PROJECT.md` — 도메인·스택·컴포넌트·용어 |
| **첫 제품 코드·새 컴포넌트·경계 변경 전** | `dev/COMPONENTS.md` — 분류 제안과 사용자 결정 기록의 계약. 그래프 정본은 `docs/architecture/components.json` |
| **새 프로젝트 조립·스택 변경** | `dev/workflows/harness-assembly.md` — 언어 선택부터 첫 분류와 검사 연결까지 |
| `.py` 파일 (모든 Python) | `dev/DEVGUIDE.md` 허브 → 해당 `dev/` 서브MD |
| `frontend/src/` React (`.tsx`·`.ts`) | `design/DESIGN_GUIDE.md` 허브 → 해당 `design/` 서브MD |
| **화면 작업 (예외 없음)** | `design/RESPONSIVE.md` — 데스크톱과 모바일을 plan에서 함께 정한다 |
| DB 스키마·테이블·컬럼 신설 | `dev/DATA_MODEL.md` — 테이블 정의 정본 |
| 테스트 작성·전략 | `dev/TESTING.md` |
| **신규 파일·함수 작성 전 (레이어 불문)** | `dev/CONVENTIONS.md` — 관례 레지스트리와 공용 헬퍼 표 |
| **기존 데이터셋을 새 표·차트·카드에 얹기 전** | `dev/CONVENTIONS.md` 「데이터셋 표시 규약」 — 순서·라벨은 화면이 정하지 않는다. 표에 없는 축이면 소비처를 grep 해 관례를 확인하고 표에 등재한다 |
| **MD 작성·수정 전 (예외 없음)** | `dev/MD_STANDARD.md` — 3원칙과 성분 판정 |
| 하네스(훅·에이전트·스킬·게이트) 파악·수정 | `dev/HARNESS.md` — 전체 지도 |
| **아키텍처·흐름 그림 작성·갱신** | `dev/DIAGRAM.md` — 정본 위치·1:1 매핑 규약·검사 48. 작성 계약은 `dev/DIAGRAM_AUTHORING.md` |
| **규칙에 이의 제기 전** | `dev/LESSONS.md` — 그 규칙이 생긴 사고 경위 |
| **이미 나온 제안을 다시 꺼내기 전** | `dev/REJECTED.md` — 거절·보류된 것과 그 사유 |
| **3단계 구현 착수 전 (예외 없음)** | `workboard/README.md` — 착수 라우팅·서식. 열린 과업은 SessionStart가 주입하나 오래된 세션은 `ls workboard/`로 다시 본다 |
| **다음 과업을 고를 때** | `docs/BACKLOG.md` — 잔여 백로그. 착수 전 서술을 실물로 대조한다 |

제품 코드는 승인된 컴포넌트와 역할에 정확히 하나씩 속한다. 새 최상위 패키지도 예외가 아니다 — 그래프에 없는 코드는 `component_classification` 이 `[DECISION]` 으로 잡고, 사용자 결정 없이는 `--verify` 를 통과하지 못한다. 분류 제안과 기록 절차의 정본은 `dev/COMPONENTS.md` 다.

---

## 4단계 기능 개발 워크플로우

**모든 기능 추가·수정·버그 수정은 아래 순서대로.** 단순 오탈자와 설정값 1줄 변경만 예외다.

**0** 범위 인터뷰 → **1** 리서치 → **2** 계획 → **2-1** 검토·반복 — **여기까지 코드 수정 금지** → **3** 구현 → **4** 수정·롤백. **3단계부터만 코드를 만진다.**

- **0단계 범위 인터뷰** — 리서치 전에 결정 지점을 AskUserQuestion으로 먼저 묻는다. 스코프 경계와 노출 위치와 적재 방식처럼 답에 따라 결과물이 달라지는 것들이다. 임의로 권고안을 정해 plan으로 미루는 것보다 먼저 묻는 게 기본이다.
- **브리프 패스트트랙** — 요청에 스코프와 노출 위치와 적재 방식이 이미 담겼으면 인터뷰를 생략하고 바로 리서치로 간다. 다 갖춘 요청에 형식적으로 되묻는 것은 절차 준수가 아니라 지연이다.
- **이견 병기** — 사용자 지시가 내 기술 판단과 다르면 지시대로 이행하되 착수 시점에 이견 1줄과 근거 1줄을 명시한다. 이견이 없으면 침묵한다. 설계를 바꿔야 할 수준이면 먼저 묻는다.
- 1~2단계 산출물은 `docs/tasks/research_<과업>.md`와 `plan_<과업>.md`다. **채팅창 요약 금지**, 파일로 남긴다.
- **시안(목업)은 아티팩트 금지** — 화면·페이지 시안은 Claude 아티팩트로 게시하지 않는다. `docs/tasks/mockup/` 밑에 파일로 만들어 승인을 받고, 승인된 목업 그대로 구현한다. 완료 시 채택분은 research·plan과 같은 archive 폴더로 옮기고 반려분은 지운다 — 채택 시안은 "왜 이 화면이 이렇게 생겼나"의 유일한 기록이다. 판단이 세션을 넘겨 이어지는 검토 중 시안만 `wip_` 접두로 예외 표시한다. Stop 훅 `check_mockup_residue.py`가 잔존 시 종료를 막는다.
- **plan 승인 전 구현 절대 금지.** 구체적인 제안과 범위를 이미 승인받았다면 그 근거를 plan에 기록하고 진행한다.
- 같은 범위의 승인을 반복해서 요구하지 않는다.
- 구현 완료 즉시 같은 턴에 `docs/tasks/archive/YYYY-MM-DD-{작업명}/`으로 `git mv`한다. "나중에 정리"는 오지 않는다.
- **"깃에 올려라"** = `git push origin HEAD` 한 번에 실행. 중간 확인 없음. 단 exit 0과 출력을 확인하기 전에 "올렸다"고 선언하지 않는다. 원격에 PR·CI가 있는 프로젝트는 `workboard/README.md` 「완료」의 push→PR→CI→merge 분기를 따른다.

**🚫 허락 구하기 금지** — 사용자에게 질문하는 지점은 **딱 두 곳**이다. 0단계 범위 인터뷰와 plan 승인 대기. 컴포넌트 분류 제안은 plan 의 「분류 제안」 행에 실어 plan 승인 때 함께 받는다. plan 밖에서 `[DECISION]` 이 나오면 그 변경만 보류하고 사용자에게 알린 뒤 독립 작업을 계속한다(정본: `dev/workflows/feature-workflow.md`). 그 외 전 구간은 자율 진행 후 결과만 보고한다. "이렇게 해도 될까요"류의 중간 확인은 금지다.

**🚫 명령어 외우게 하기 금지** — 사용자가 스킬 이름·프리셋 이름·검사 번호를 알아야 뭔가 돌아가는 상태를 만들지 않는다. 때가 됐는지는 하네스가 재고, 무엇을 쓸지는 내가 고른다. 사용자에게 남는 질문은 **자기만 아는 것**뿐이다 — 무엇을 만들고 싶은지, 이 분야에서 무엇이 의미 있는지, 레포 주소가 무엇인지.

---

## 증거·경계 원칙

> 각 항목의 사고 경위는 `dev/LESSONS.md`의 해당 절에 있다. 규칙이 과해 보이면 먼저 읽는다.

1. **주장엔 외부 증거** — DB 상태는 쿼리로, 함수 동작은 Read로, 파일 존재는 Glob과 Grep으로 확인한 뒤 말한다. 미확인이면 "확인이 필요하다"고 명시한다. §2
2. **완료 선언은 검증 출력과 함께** — 검증 명령의 exit code와 출력이 주장을 확인할 때만 선언한다. UI는 렌더된 화면까지 본다. "통과할 것"과 "이제 동작할 것"은 금지다. §11
2-1. **검증 방법이 그 변경의 실패 모드를 실제로 건드리는지 묻는다** — 초록불 셋이 깨진 코드를 통과시킨 사례가 있다. 라우트를 바꿨으면 핸들러를 호출하고, 인증 엔드포인트의 401을 정상 동작의 증거로 읽지 않는다.
3. **시그니처·API 응답·테이블을 바꾸면 소비자를 추적한다** — 승인된 그래프의 간선을 따라 정책에서 어댑터·전달·화면으로 흐른다. 추적은 **grep으로** 한다 — import 성공은 추적이 아니다. 본문 안의 이름은 import를 통과하고 호출 시점에 터진다. 확인 없이 "영향 없음"이라고 말하지 않는다. §3
4. **스테일 Read로 편집 금지** — 컨텍스트 압축 경고가 뜬 Read는 무효다. 편집 전 재Read한다. §1
5. **UI 신규·수정은 전부 `frontend/src/` React** — API만 만들고 소비 컴포넌트를 안 만들면 사용자에게 안 보이는 orphan이다. §8
6. **DB 변경 전 `dev/DATA_MODEL.md` 먼저** — 불변 속성은 차원 한 곳에 두고 팩트는 denormalize하지 말고 JOIN한다. §9
7. **디버깅은 단일 가설과 최소 변경, 3회 실패 시 STOP** — 증거와 함께 사용자에게 보고한다. §10
8. **research·plan 생략 금지** — 예외 기준은 「4단계」 첫 줄 하나다. "간단해 보인다"는 생략 근거가 아니다. §4 §0
9. **병렬 체제에서 구현·커밋은 자기 worktree에서만** — 첫 편집 전에 브랜치와 경로를 확인하고, 아니면 멈춘다. 프로토콜 정본은 `workboard/README.md`다.

---

## 일관성 게이트 (anti-spaghetti)

> 산문 규칙은 세션이 길어지면 흘러내린다. 검사 가능한 규칙을 산문으로 단속하는 것 자체가 실패 메커니즘이다. 진단 전문은 `dev/LESSONS.md`.

1. **검사 가능한 규칙은 게이트로 강제** — 작성 시점 PostToolUse와 종료 시점 Stop 두 층이다. 판정 정본은 `kernel/gates/`, 이 프로젝트의 이름·어휘·예외의 정본은 `harness_profile.py`, 사람용 지도는 `dev/HARNESS.md`다. 새 검사가능 규칙은 MD 문장 추가가 아니라 커널에 판정을 더하거나, 이 레포에만 필요하면 `harness_gates/<이름>.py`에 심는다. 산문 드리프트는 월 1회 `/md-audit`이 상한선이다.
   **`[SKIP]`은 통과가 아니다.** 게이트 출력에 `[SKIP]`이 보이면 그 규칙은 지금 아무것도 안 지켜주고 있다. 프로파일을 채울 수 있는 항목이면 채운다.
2. **게이트 우회 금지** — 소스 생성·수정은 반드시 Edit와 Write로 한다. Bash 리다이렉트는 작성 시점 게이트를 우회한다. 400줄을 기계 분할로 때우지 않는다. 분할은 기능 단위다. 타입을 `Any`나 `any`로 때우지 않는다. 불가피하면 사유를 인라인 주석으로 남긴다. **게이트는 하한선이지 목표가 아니다.**
3. **래칫 원칙** — 대규모 정리는 그 상태를 잠그는 검사와 같은 커밋으로 끝낸다. baseline 파일은 감소만 허용한다.
4. **발명 금지, 모방 강제** — 새 코드 전에 `dev/CONVENTIONS.md`와 레이어 MD의 정본 예시를 Read해 그 형태를 모방한다. 갈리는 선택은 `dev/CONVENTIONS.md`에 등재한다. 일관성이 개인 취향보다 우선한다. 더 나은 패턴이 있으면 먼저 물어 정본을 바꾼 뒤 전파한다.

---

## 코딩·편집 원칙

**편집 잠금** — 3단계 시작 시 `workboard/`를 확인하고 자기 과업 파일을 등록한다(파일명 = 수정 범위, git 비추적). 같은 범위가 이미 있으면 새로 파지 말고 합류하거나 쌓는다. 서식과 `#sid`와 착수 라우팅·병렬 프로토콜의 정본은 `workboard/README.md`다.

**리서치(1단계)** — 파일명과 시그니처만 훑지 않는다. 내부 로직과 데이터 흐름과 모듈 의존성까지 본다. 소스 수정은 절대 금지다.

**계획(2단계) 필수 항목의 정본은 [공통 기능 변경 절차](dev/workflows/feature-workflow.md)다.** 접근 방식과 실제 코드 스니펫과 정확한 파일 경로 위에 파일 설계표와 반응형 설계표와 행동 검증 테스트와 병렬 후보와 아키텍처 델타가 전부 있어야 plan이다. 없으면 구현 중에 게이트를 만난다 — 계획하지 않은 것을 구현하다 만나는 게이트는 마찰이지 방어가 아니다.
**No Placeholders** — "TBD"나 "나중에 구현"이나 "위와 유사하게"나 정의되지 않은 참조가 있으면 plan 실패다.

**구현(3단계)** — plan Todo를 순서대로 실행하고 완료마다 체크한다. 모든 Todo가 끝날 때까지 멈추지 않는다. 타입체크와 테스트를 지속적으로 돌린다. 완료 근거는 `python -X utf8 -m kernel.runner --verify` exit 0 뿐이다 — `[TOOL]` 과 결정 대기(exit 3)는 완료가 아니다.

**단순성 우선** — Code Ladder와 근본 원인 수정과 비타협 안전의 전문은 `.claude/hooks/code-master.md`가 SessionStart에 주입한다. 요청 범위를 넘는 기능과 단일 사용 추상화와 불가능한 시나리오의 에러 핸들링은 만들지 않는다. `debt:` 부채는 월 1회 `/code-debt`로 수확한다.

**외과적 변경** — 인접 코드와 주석과 포맷을 "개선"하지 않는다. 고장나지 않은 것은 리팩터하지 않는다. 내 스타일과 달라도 기존 스타일에 맞춘다. 관련 없는 dead code는 언급만 하고 삭제하지 않는다. 기준은 **변경된 모든 줄이 사용자 요청으로 직접 추적 가능한가**다.

**응답과 산출물 길이** — 분량은 답의 본체에 쓴다. 단서와 면책은 짧게 유지한다. 설명 요청에는 개괄로 답하고 심층 설명은 따로 요청받았을 때만 쓴다. 디스크에 쓰는 산출물도 같은 기준이다. 실질은 담되 채움 절과 중복 요약과 상투 문구로 늘리지 않는다.

---

## MD 시냅스 관리

**MD는 장기 기억이자 작동 규칙이다.** 변경한 영역의 정본 MD를 같은 턴에 갱신하되 반드시 `dev/MD_STANDARD.md` 3원칙으로 쓴다. grep으로 알 수 있는 것은 적지 않는다. 어느 MD인지는 라우팅표의 역방향이다.

사용자의 지적과 피드백도 즉시 MD로 남긴다. 사고성이면 `dev/LESSONS.md`에 경위까지, UX 원칙이면 `design/UX.md`에 넣는다. **"안정화 후 일괄"의 순간은 오지 않는다.**

**LESSONS에 남긴 사고는 게이트 후보다.** 경위를 적은 뒤 기계로 검사 가능한지 판단해서, 가능하면 같은 턴이나 다음 턴에 `kernel/gates/`에 판정을 더하고 그 절의 `> 강제:`를 게이트 번호로 갱신한다. 불가능하면 `산문 전용`과 사유를 적는다. 판단 자체를 미루면 검사 30이 막는다. **이 순환이 하네스가 이 프로젝트에 맞게 촘촘해지는 유일한 경로다** — 게이트는 설계된 게 아니라 사고마다 하나씩 늘었다.

이 프로젝트에서만 참인 규칙은 커널이 아니라 `harness_gates/`에 심는다. 커널은 남의 프로젝트 규칙을 이고 가지 않는다.

자명하지 않은 갱신처만 적는다. 새 화면은 `design/DESIGN_GUIDE.md` 계열, DB 스키마는 `dev/DATA_MODEL.md`, 도메인 지식은 `PROJECT.md`, 하네스 구성은 `dev/HARNESS.md`다. 각각 게이트가 코드 실물과 대조한다.

**새 MD**를 만들면 같은 턴에 허브 라우팅표에 행을 추가한다. 없으면 읽힐 타이밍이 없고 검사 27이 고아로 잡는다.

---

## 모델 라우팅 — 토큰 볼륨 × 판단 스테이크

메인 루프는 **Opus 5**다. 1M 컨텍스트라 정본 MD와 소스는 직접 Read가 기본이다. 위임은 비용 회피 수단이 아니라 **독립·병렬 대규모 작업의 병렬화 수단**이다.

| 역할 | 모델 | 언제 |
|------|------|------|
| 메인 루프 — 계획·구현·리서치·검수 | Opus 5 | 상시 |
| plan 통째 실행 — 3단계 구현 | Fable 5 에이전트 (executor) | 승인된 plan이 파일 8개 이상이거나 레이어 3개 이상 횡단 |
| 대규모 병렬 구현 지휘 | Fable 5 에이전트 (orchestrator) | 승인된 plan이 서로소 트랙 3개 이상. 소유권 배정·공유 파일 직렬 적용의 정본은 에이전트 파일 |
| 고스테이크 단발 판단 | Opus 5 메인 루프 + effort 상향 | plan 최종 검토·스키마 설계·디자인 채택 |
| 대량 기계적 정독·팬아웃 | Sonnet 5 에이전트 | 수십 파일 독립 스캔 |

- **스폰 최소화** — 몇 번의 툴 호출로 끝날 일은 위임하지 않는다. 에이전트 부트스트랩 비용이 더 크다. 오케스트레이션 발동 조건은 겹침을 공유로 뺀 뒤 서로소 트랙 3개 이상이고, **큰데 순차인 과업은 발동하지 않는다.**
- **오케스트레이션 중에도 git·검증·보드는 메인 세션이 전담한다. 워커 보고는 증거가 아니다.**
- **단발 위임의 입력은 결정에 필요한 요약과 파일 경로다.** 원본을 대량으로 주입하지 않는다.
- **effort가 손잡이다** — 비용 절감은 effort 하향이지 모델 다운그레이드가 아니고, 판단을 올리는 수단도 모델 교체가 아니라 effort 상향이다. 장황함은 프롬프트로 제어한다. 되돌리기 비싼 결정에 검증 전용 에이전트를 붙이지 않는다 — Opus 5는 시키지 않아도 자기 작업을 검증하므로 중복 검증은 토큰만 쓰고 결과를 바꾸지 않는다. effort의 정본은 각 에이전트 frontmatter다.
- **Fable 위임은 절차가 아니라 목표로 준다.** 단계를 나열한 브리프는 장기 자율 실행의 품질을 떨어뜨린다. 게이트와 완료 조건은 제약이라 남기고 순서 지시만 뺀다. 브리프의 자족성은 plan의 No Placeholders 규칙이 담보한다.
- 에이전트 반환은 요약과 `detail_path` 분리다. 상세는 파일로 남기고 반환문에는 결론과 file:line 포인터만 넣는다. SubagentStop 훅이 비만 반환을 차단한다.
- **세션 분리** — 태스크가 완결되면 `/clear`한다. 프로젝트급 작업 여러 개를 한 세션에 끌고 가지 않는다.
- **Workflow 함정** — 워크플로우 스크립트의 **모든 `agent()` 호출**에 model을 명시한다. 미지정이면 메인 모델을 상속해 단가와 거부 정책이 따라간다. ⑧-4 가 호출 시점에 막고, 계약 정본은 `dev/HARNESS.md` 「Workflow 툴」이다.

---

## 모듈 소유권

| 종류 | 고유 사항 |
|------|----------|
| **동시 편집 금지** | 컴포넌트 그래프 정본과 그 승인 기록, 프로파일. 병렬 체제에서는 `workboard/` 의 「손대는 곳」이 잠금 단위다 |
| **컴포넌트 경계** | 제품 코드는 승인된 컴포넌트 하나에 속하고, 허용 의존은 그래프 간선이 정한다. 관측된 의존은 자동으로 허용되지 않는다 |
| **역할 배치** | 정책·유스케이스·포트·조회·어댑터·전달의 책임은 `dev/ARCHITECTURE.md` 가 정한다. 그래프 밖 배치는 `component_classification` 이 `[DECISION]` 으로 잡는다 |
| **환경변수** | 전부 프로파일이 선언한 설정 모듈 경유. 다른 모듈의 직접 접근은 게이트가 막는다 |

의존 방향의 정본은 컴포넌트 그래프(계약은 `dev/COMPONENTS.md`)이고, 코드 관례는 `dev/DEVGUIDE.md`다.
**그래프에 없는 역방향 import는 절대 금지.**

---

## 하네스 등록

에이전트와 스킬은 세션에 자동 주입된다. 정본은 각 디렉토리 파일이고 **사람용 전체 지도는 `dev/HARNESS.md`**다. 하네스 구성이 바뀌면 같은 턴에 지도를 갱신하고 검사 28이 실물과 대조한다. 모델과 effort는 각 에이전트 frontmatter가 정본이고 검사 29가 프로파일의 정책표와 대조한다 — 한쪽만 바꾸면 막힌다.

## 정비 — 하네스가 때를 알린다

월간 감사류(`md-audit`·`code-audit`·`code-debt`·`impeccable critique`·`review-loop`)는 사용자가 시켜서 도는 게 아니다. SessionStart 훅이 레포 실물에서 임계치를 재고(마지막 실행 후 커밋 수·경과일·바뀐 화면 파일·남은 `debt:` 표시) 넘으면 `[정비]` 로 알린다. 판정 정본은 `kernel/maintenance.py`이고 임계치는 프로파일의 `MAINTENANCE`가 조정한다.

**알림이 뜨면 묻지 말고 실행한다.** 다만 현재 요청을 먼저 끝내고, 그다음 같은 세션에서 돌린다. 전부 보고서만 내고 코드는 고치지 않으므로 승인이 필요 없다. 끝나면 `python -X utf8 -m kernel.maintenance --stamp <이름>`으로 기록하고 `harness_maintenance.json`을 커밋한다 — 기록이 공유돼야 주기가 성립한다.

발견분을 고칠지는 그다음 문제다. 보고서를 요약해 사용자에게 올리고, 고치라고 하면 그때 `/feature-workflow`를 탄다.

---

<tone_preference>
출력은 적당히 간결하게 유지한다.
</tone_preference>
